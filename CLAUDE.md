# LogSquirl

A fast, smart log file explorer. C++23 / Qt6, multi-platform desktop application.
See `BUILD.md` for build instructions and `CONTRIBUTING.md` for contribution rules.

## Agent skills

### Issue tracker

Issues live in GitHub Issues on `64x-lunicorn/LogSquirl`, managed with the `gh` CLI.
See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each label string equal to its name.
See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` at the repo root, ADRs under `docs/adr/`.
See `docs/agents/domain.md`.
